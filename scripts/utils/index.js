const {
    ENV_KEY,
    RPC_URL,
    DEPLOYER_PK,
    DEPLOYER_ADDR,
    Provider,
    Deployer,
    DeploymentStorage,
    CoinBase,
} = require("./env");

const { log } = require("console");

module.exports = {
    log,
    ENV_KEY,
    RPC_URL,
    DEPLOYER_PK,
    DEPLOYER_ADDR,
    Provider,
    Deployer,
    DeploymentStorage,
    CoinBase
};
